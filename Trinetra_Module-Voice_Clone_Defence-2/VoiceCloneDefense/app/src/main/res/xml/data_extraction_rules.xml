<?xml version="1.0" encoding="utf-8"?>
<!--
  Voiceprints are wrapped by an Android Keystore key that is non-exportable and
  device-bound. Backing the database up would produce ciphertext that can never be
  decrypted again, so both cloud backup and device-transfer are disabled outright.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
