<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      SCOPE BOUNDARY (deliberate, not a temporary limitation):
      This app captures audio ONLY from MediaRecorder.AudioSource.MIC, while a call is on
      speakerphone. It never requests, and must never request, VOICE_CALL / VOICE_UPLINK /
      VOICE_DOWNLINK / VOICE_COMMUNICATION-for-call-tap sources. Those are signature-only on
      Android and are out of scope by design. There is deliberately no CAPTURE_AUDIO_OUTPUT,
      no READ_PHONE_STATE, no PROCESS_OUTGOING_CALLS, and no InCallService here.

      NETWORK, and what changed: the app now holds INTERNET, because the VoIP module carries a
      WebRTC call and a call is a network operation by definition. The earlier claim that this app
      had no network capability at all is no longer true and has been removed rather than left
      standing.

      What has not changed is where inference happens. No audio, embedding, or derived signal is
      sent anywhere for analysis; the models run on-device and there is no server to send to. The
      only audio that leaves the device is the user's own microphone, going to the person they
      chose to call — which is what a phone call is. Signalling is a direct socket to the other
      device on the local network; media is DTLS-SRTP encrypted by WebRTC.
    -->

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
      Read-only, and only to populate the dialler list so calls can be placed by name instead of
      by IP address. Contact names are never stored, matched, or sent anywhere; the call itself
      still goes to a device on the local network, not to a phone number.
    -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- An incoming call has to take over the screen; a banner is not a ringing phone. -->
    <uses-permission
        android:name="android.permission.USE_FULL_SCREEN_INTENT"
        android:minSdkVersion="34" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"
        android:minSdkVersion="34" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:minSdkVersion="33" />

    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:name=".VcdApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceCloneDefense">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.VoiceCloneDefense">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.LiveVerificationService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!--
          A VoIP call holds the microphone for its whole duration. Declared as a microphone
          foreground service rather than phoneCall, because phoneCall type requires
          MANAGE_OWN_CALLS and a ConnectionService — telephony integration this module
          deliberately does not have.
        -->
        <service
            android:name=".service.VoipCallService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

    </application>
</manifest>
